version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: music-feedback-website
    env_file:
      - ./config/.env
    volumes:
      - .:/var/www/html
    ports:
      - "80:80"
